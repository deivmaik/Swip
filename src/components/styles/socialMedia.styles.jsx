import styled from 'styled-components'
import { colors } from '../../global'



// Social Media styles 
export const FollowUs = styled.p`
display: block; 
`

// This creates a generic color for a whole block
export const SocialMediaList = styled.div`
  font-weight: 400;
  color: ${colors.light};
`

export const SMItem = styled.div`
display: block;
`